{
  "name": "cratedigger-demo",
  "version": "1.0.0",
  "description": "3D vinyl records exploration demo using Three.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npm run copy-to-docs",
    "copy-to-docs": "rimraf docs && mkdir docs && cp -r dist/* docs/ && cp cratediggerDB.csv docs/ && echo '' > docs/.nojekyll"
  },
  "dependencies": {
    "three": "^0.158.0",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}